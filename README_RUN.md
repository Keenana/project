# GoClimb Mobile (UI demo)

## Requirements
- Node.js 18+
- pnpm, yarn or npm

## Install
```bash
yarn install # or: npm install
```

## Run (Expo)
```bash
yarn start
```
Then press `a` for Android emulator or `i` for iOS (on macOS). Scan QR code in Expo Go for a real device.
